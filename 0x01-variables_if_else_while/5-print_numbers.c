#include <stdio.h>
/*
 * Description: This program prints
 * single digits
 */
/**
 * main - the function has the loop
 * and the print
 *
 * Return: always 0
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d\n", n);
	}
	return (0);
}
